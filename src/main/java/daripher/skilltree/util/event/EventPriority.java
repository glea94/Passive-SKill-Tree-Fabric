package daripher.skilltree.util.event;

/** Reproduit net.minecraftforge.eventbus.api.EventPriority : ordre d'exécution HIGHEST -> LOWEST. */
public enum EventPriority {
    HIGHEST, HIGH, NORMAL, LOW, LOWEST
}
